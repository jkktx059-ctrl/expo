import React, { useState } from 'react';
import { View, Text, ScrollView, TouchableOpacity, StyleSheet, TextInput } from 'react-native';
import { NavigationContainer } from '@react-navigation/native';
import { createBottomTabNavigator } from '@react-navigation/bottom-tabs';

const Tab = createBottomTabNavigator();

// Home Screen
function HomeScreen() {
  return (
    <ScrollView style={styles.container}>
      <Text style={styles.title}>Home Feed</Text>
      <View style={styles.card}>
        <Text>User1 watched Stranger Things</Text>
        <TouchableOpacity style={styles.button}>
          <Text style={styles.buttonText}>React / Comment</Text>
        </TouchableOpacity>
      </View>
      <View style={styles.card}>
        <Text>User2 asks: What should I watch next?</Text>
      </View>
    </ScrollView>
  );
}

// Discover / Search Screen
function DiscoverScreen() {
  const [search, setSearch] = useState('');
  const placeholderShows = ['Stranger Things', 'Modern Family', 'Inception', 'Avatar'];

  return (
    <ScrollView style={styles.container}>
      <Text style={styles.title}>Discover</Text>
      <TextInput
        style={styles.input}
        placeholder="Search shows or movies"
        value={search}
        onChangeText={setSearch}
      />
      {placeholderShows
        .filter((item) => item.toLowerCase().includes(search.toLowerCase()))
        .map((show, index) => (
          <View key={index} style={styles.card}>
            <Text>{show}</Text>
            <TouchableOpacity style={styles.button}>
              <Text style={styles.buttonText}>Add to My Lists</Text>
            </TouchableOpacity>
          </View>
        ))}
    </ScrollView>
  );
}

// Status Screen
function StatusScreen() {
  return (
    <ScrollView style={styles.container}>
      <Text style={styles.title}>Status</Text>
      <View style={styles.card}>
        <Text>Total Movies Watched: 12</Text>
        <Text>Total Shows Completed: 5</Text>
        <Text>Top Genre: Sci-Fi</Text>
      </View>
      <View style={styles.card}>
        <Text>Monthly Recap:</Text>
        <Text>- Stranger Things: 2 episodes</Text>
        <Text>- Modern Family: 3 episodes</Text>
      </View>
      <View style={styles.card}>
        <Text>Yearly Recap:</Text>
        <Text>- Total Movies: 40</Text>
        <Text>- Total Shows: 15</Text>
      </View>
    </ScrollView>
  );
}

// My Picks Screen
function MyPicksScreen() {
  const [topShows, setTopShows] = useState(['Stranger Things', 'Modern Family', 'Inception']);
  const [topMovies, setTopMovies] = useState(['Inception', 'Avatar', 'Interstellar']);

  return (
    <ScrollView style={styles.container}>
      <Text style={styles.title}>My Picks</Text>
      <Text style={styles.subtitle}>Top Shows</Text>
      {topShows.map((show, index) => (
        <View key={index} style={styles.card}>
          <Text>{index + 1}. {show}</Text>
        </View>
      ))}
      <Text style={styles.subtitle}>Top Movies</Text>
      {topMovies.map((movie, index) => (
        <View key={index} style={styles.card}>
          <Text>{index + 1}. {movie}</Text>
        </View>
      ))}
    </ScrollView>
  );
}

// Profile Screen
function ProfileScreen() {
  return (
    <ScrollView style={styles.container}>
      <Text style={styles.title}>Profile</Text>
      <View style={styles.card}>
        <Text>Username: MovieFan123</Text>
        <Text>Bio: I love binge-watching shows</Text>
      </View>
      <View style={styles.card}>
        <Text>Watch History:</Text>
        <Text>- Stranger Things (Completed)</Text>
        <Text>- Inception (Completed)</Text>
      </View>
    </ScrollView>
  );
}

// App container
export default function App() {
  return (
    <NavigationContainer>
      <Tab.Navigator screenOptions={{ headerShown: false }}>
        <Tab.Screen name="Home" component={HomeScreen} />
        <Tab.Screen name="Discover" component={DiscoverScreen} />
        <Tab.Screen name="Status" component={StatusScreen} />
        <Tab.Screen name="My Picks" component={MyPicksScreen} />
        <Tab.Screen name="Profile" component={ProfileScreen} />
      </Tab.Navigator>
    </NavigationContainer>
  );
}

// Styles
const styles = StyleSheet.create({
  container: { flex: 1, padding: 16, backgroundColor: '#fff' },
  title: { fontSize: 24, fontWeight: 'bold', marginBottom: 16 },
  subtitle: { fontSize: 18, fontWeight: 'bold', marginTop: 12, marginBottom: 8 },
  card: {
    backgroundColor: '#f5f5f5',
    padding: 16,
    marginBottom: 16,
    borderRadius: 16,
  },
  button: {
    marginTop: 8,
    backgroundColor: '#d4a373',
    padding: 8,
    borderRadius: 12,
    alignSelf: 'flex-start',
  },
  buttonText: { color: '#fff', fontWeight: 'bold' },
  input: {
    backgroundColor: '#eaeaea',
    padding: 12,
    borderRadius: 12,
    marginBottom: 16,
  },
});
